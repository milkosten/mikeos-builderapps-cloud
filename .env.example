# mikeos-builderapps-cloud — real values live in .env on 242 (never committed).
BUILDERAPPS_DB_PASSWORD=change_me
DATABASE_URL=postgresql://mikeos:change_me@mikeos-builderapps-db:5432/mikeos_builderapps

GITEA_URL=https://gitea.osmike.com
GITEA_ADMIN_USER=mikeadmin
GITEA_ADMIN_TOKEN=              # from /opt/builderapps/gitea/.env

OLLAMA_GPU_URL=ollama://mikeos:PASS@81.8.177.182:11443
OPENROUTER_API_KEY=            # from /opt/mikeos-designer-cloud/.env (Kimi authorized for builderapps)
BUILDERAPPS_LLM_MODEL=moonshotai/kimi-k3

CHROME_POOL_URL=https://chrome-pool.osmike.com
CHROME_POOL_USER=mikeos
CHROME_POOL_PASS=

MIKEOSCOMPUTERS_URL=https://account.osmike.com
ACCOUNT_OSMIKE_ISSUER=https://account.osmike.com

# generate: python -c 'from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())'
SECRETS_KEY=

PUBLIC_BASE=https://builderapps.osmike.com
SITES_BASE=builderapps.osmike.com
CORS_ORIGINS=https://builderapps.osmike.com
