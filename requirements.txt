# HTTP API
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Async PostgreSQL
asyncpg>=0.29.0

# GPU (Ollama) / OpenRouter + legacy X-API-KEY resolver + Gitea/docker health calls
httpx>=0.26.0

# account.osmike.com RS256 JWTs via JWKS
pyjwt[crypto]>=2.8.0
cryptography>=42.0.0

# compose normalizer (parse/rewrite docker-compose.yml)
PyYAML>=6.0

# phase 34: the Discuss room reads PDFs the user points it at. chrome-pool downloads the
# bytes; this extracts the text, because Chrome renders a PDF in a viewer whose DOM snapshot
# says nothing about the document.
pypdf>=4.2.0
